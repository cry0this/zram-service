[Unit]
Description=Zram-based swap (compressed RAM block devices)

[Service]
Type=oneshot
ExecStart=/usr/local/bin/zramctl start
ExecStop=/usr/local/bin/zramctl stop
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
