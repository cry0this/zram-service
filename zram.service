[Unit]
Description=Zram-based swap (compressed RAM block devices)

[Service]
Type=oneshot
ExecStart=/usr/bin/zramctl start
ExecStop=/usr/bin/zramctl stop
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
